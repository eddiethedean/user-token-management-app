# Archived reference scripts

`transfer_code/` held the standalone MSS/MCS-COP/PostgreSQL scripts used as protocol evidence
for Real Transfer Integration. Production code must not import this package. Connector behavior
lives in `app/connectors/`.

These files are retained only as historical evidence. Do not copy operational RIDs, tokens, or
hostnames from them into source.
